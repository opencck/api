{
	"name": "opencck-api",
	"version": "1.0.2",
	"description": "JSONRPC API backend based on Doctrine (DBAL)",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "Krupkin Sergey",
	"devDependencies": {
		"@prettier/plugin-php": "^0.14.0",
		"prettier": "^2.0.4"
	}
}
